# Offly marketing site

Static HTML/CSS landing pages for Offly — the private baby journal and offline
memory vault.

## Local preview

From the repo root, run:

```bash
python3 -m http.server 8000
```

Then open:

```
http://localhost:8000/offly/
```

You can also open `index.html` directly in a browser.

## Update checklist

- Open Graph image in `assets/og-image.svg` if branding changes
- Support email stays `support@qrodesk.com`
- Terms use Apple's Standard EULA link
- Privacy copy matches the current app build
